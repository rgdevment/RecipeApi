version: '3.9'

services:
  postgres:
    image: postgres:16.2-alpine
    ports:
      - "5432:5432"
    volumes:
      - ~/apps/postgres:/var/lib/postgresql/data
    environment:
      - POSTGRES_PASSWORD=MyPr35m1umS35cr35t
      - POSTGRES_USER=recipe
      - POSTGRES_DB=recipe_api
